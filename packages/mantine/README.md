# @mdl/mantine

Mantine-based MDL registry implementations for `@mdl/react`.

This package intentionally does **not** render a `MantineProvider` and does **not** import Mantine CSS. Applications consuming this package must provide their own `MantineProvider` and import the Mantine styles they need.
